<nav class="navbar sticky-top navbar-expand-lg navbar-dark bg-dark">
    <div class="container-fluid">
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarTogglerDemo03"
            aria-controls="navbarTogglerDemo03" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <a class="navbar-brand" href="/">Compass</a>
        <div class="collapse navbar-collapse" id="navbarTogglerDemo03">
            <div class="navbar-nav">
                <ul class="navbar-nav">
                    <li class="nav-item">
                        <a class="nav-link" href="/">Home</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/campgrounds">Camps</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/campgrounds/new?newcamp=True">New Camp</a>
                    </li>
                </ul>
            </div>
            <div class="navbar-nav ml-auto">
                <% if (!currentUser){ %>
                    <a class="nav-link" href="/register">Sign up</a>
                    <a class="nav-link" href="/login">Sign in</a>
                    <% } else{ %>

                        <a class="nav-link" href="/logout">Sign out</a>
                        <% } %>

            </div>
        </div>

        <div>
            <% if (currentUser){ %>
                <a class="pull-right" href="/profile/<%= currentUser._id%>"><img class="rounded-circle" width="30"
                        src="/images/profile-picture.png" alt="IMAGE"></a>
                <% } %>

        </div>
    </div>

</nav>